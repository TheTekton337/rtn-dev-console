name: 'CodeQL Analysis'

on:
  push:
    branches:
      - main
      - development
  pull_request:
    branches:
      - main
      - development
  schedule:
    - cron: '0 14 * * 1'

jobs:
  analyze:
    name: Analyze
    runs-on: macOS-latest
    permissions:
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: ['javascript', 'typescript', 'objective-c', 'swift']
        # language: ['javascript', 'typescript', 'objective-c', 'swift', 'java', 'kotlin']
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build package
        run: yarn prepare

      # - name: Build the Project
      #   run: xcodebuild -workspace example/ios/RtnDevConsoleExample.xcworkspace -scheme RtnDevConsoleExample build

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2
